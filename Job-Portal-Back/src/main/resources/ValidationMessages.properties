# User validation messages
user.name.absent = Name is null or blank.
# Validation message shown when user's name field is empty or contains only whitespace

user.email.absent = Email is null or blank. 
# Validation message shown when user's email field is empty or contains only whitespace

user.email.invalid = Email is invalid.
# Validation message shown when user's email does not match valid email format

user.password.absent = Password is null or blank.
# Validation message shown when user's password field is empty or contains only whitespace

user.password.invalid = Password is invalid.
# Validation message shown when user's password does not meet complexity requirements

# OTP validation messages
otp.invalid = OTP is invalid.
# Validation message shown when provided OTP code is incorrect or expired