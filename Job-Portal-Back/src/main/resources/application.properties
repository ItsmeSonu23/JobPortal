# Application Name Configuration
spring.application.name=Job-Portal
# The name of the Spring application is set to "Job-Portal".

# MongoDB Configuration
spring.data.mongodb.auto-index-creation=true
# Enables automatic creation of indexes in MongoDB during application startup.

spring.data.mongodb.uri=mongodb+srv://asasino3180:5KvCtoZ8meSpGeuf@cluster0.i6pjj.mongodb.net/jobportal?retryWrites=true&w=majority&appName=Clover
# Connection URI to the MongoDB database (localhost on port 27017 with database "jobportal").

# Mail Configuration
spring.mail.host=smtp.gmail.com
# The mail host is set to Gmail's SMTP server (smtp.gmail.com).

spring.mail.port=587
# SMTP server port is set to 587, which is the default for Gmail's secure TLS connection.

spring.mail.username=asasino3180@gmail.com
# The email address used to authenticate with the Gmail SMTP server for sending emails.

spring.mail.password=mpfksukyxdkkcldp
# The password used for authenticating with the Gmail SMTP server.

spring.mail.properties.mail.smtp.auth=true
# Enables SMTP authentication for sending emails via Gmail's SMTP server.

spring.mail.properties.mail.smtp.starttls.enable=true
# Ensures a secure connection via TLS (STARTTLS) for Gmail's SMTP server.

# Custom Error Messages
USER_FOUND=User has already registered.
# Error message shown when attempting to register with an existing email ID.

USER_NOT_FOUND=User not found with this email id.
# Error message shown when no user exists with the provided email ID during login.

INVALID_CREDENTIALS=Invalid Credentials
# Error message shown when user provides incorrect login credentials.

OTP_NOT_FOUND=OTP is expired!
# Error message shown when the OTP has expired and is no longer valid.

OTP_INCORRECT=OTP is incorrect
# Error message shown when the provided OTP does not match the expected value.

JOB_NOT_FOUND=Job with this id does not exist
# Error message shown when attempting to access a job posting that does not exist.

JOB_APPLIED_ALREADY=Job already applied
# Error message shown when user attempts to apply to a job they have already applied for.

JOB_DTO_CANNOT_BE_NULL=Job DTO cannot be null
# Error message shown when the provided Job DTO is null.

JOB_STATUS_NOT_ALLOWED=Job status not allowed
# Error message shown when attempting to update a job with an invalid status.